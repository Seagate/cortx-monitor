#!/bin/bash -e

# Stop the script if any command fails
set -e -u -o pipefail

TEST_DIR=/opt/seagate/eos/sspl/sspl_test
if rpm -qa | grep -q sspl-test ;
then
    $TEST_DIR/run_tests.sh test $*
    exit $?
else
    echo "Please install sspl-test RPM"
    exit 1
fi
